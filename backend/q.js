const { Pool } = require('pg'); const pool = new Pool({ user: 'postgres', host: 'localhost', database: 'dashboard_membros', password: '252088', port: 5432 }); pool.query('SELECT id, avatar_url FROM membros WHERE id IN (''AL20251104091147-WM7I'', ''EM20251104091149-JP3O'')').then(r => { console.log('\nAVATARES:'); r.rows.forEach(row => console.log(row.id + ': ' + (row.avatar_url || 'SEM'))); pool.end(); });
